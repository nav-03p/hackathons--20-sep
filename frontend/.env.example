# Public browser token from Mapbox (starts with pk.). Leave blank to use Carto's
# no-key basemap fallback.
VITE_MAPBOX_ACCESS_TOKEN=

# Normally blank: Vite proxies /api to http://localhost:4000.
# VITE_API_URL=
# VITE_BACKEND_URL=http://localhost:4000
